[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "tabalyst"
version = "0.1.0a1"
description = "Open-source data profiling engine for structured JSON and customizable HTML reports."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    { name = "Gregory Borelli" },
]
keywords = [
    "csv",
    "data-analysis",
    "data-profiling",
    "data-quality",
    "eda",
    "html-report",
    "json",
]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Information Analysis",
]
dependencies = [
    "charset-normalizer>=3.4",
    "jinja2>=3.1",
    "pandas>=2.2",
    "pydantic>=2.10",
    "typer>=0.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-cov>=6",
    "ruff>=0.11",
]

[project.scripts]
tabalyst = "tabalyst.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
tabalyst = ["templates/*.html", "static/*.css", "static/*.js"]

[project.urls]
Homepage = "https://github.com/chergreg/tabalyst"
Repository = "https://github.com/chergreg/tabalyst.git"
Issues = "https://github.com/chergreg/tabalyst/issues"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 88
target-version = "py311"
